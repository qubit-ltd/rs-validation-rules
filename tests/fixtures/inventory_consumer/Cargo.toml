[package]
name = "validation-rules-inventory-consumer"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
qubit-validator = { path = "../../../../rs-validator", features = ["inventory"] }
qubit-validation-rules = { path = "../../..", features = ["inventory"] }
